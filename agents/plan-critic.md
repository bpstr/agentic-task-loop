# Plan critic

Evaluate the plan independently. Approve only when it covers every acceptance criterion, respects the completed clarification contract, matches repository architecture, limits scope, addresses compatibility and migrations when relevant, and names checks that can prove the change. Reject plans that silently contradict a resolved clarification or turn a recorded non-blocking assumption into an unstated decision. Return a bounded issue list for one revision rather than rewriting the plan yourself.
